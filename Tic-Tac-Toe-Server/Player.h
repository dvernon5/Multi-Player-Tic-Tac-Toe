#ifndef Player_h
#define Player_h

/* ------------------------------------------------------------------------
 * STRUCT NAME: Player
 * ------------------------------------------------------------------------
 * @brief Hold variables for row and column number.
 *
 * The Player struct is used to allow user to enter a row and column number
 * to make a move on the game board.
 * ------------------------------------------------------------------------
 */
struct Player {
  int row;
  int column;
};
#endif /* Player_h */
